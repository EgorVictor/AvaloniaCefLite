<Project Sdk="Microsoft.NET.Sdk">
    <PropertyGroup>
        <TargetFramework>net8.0</TargetFramework>
        <RootNamespace>MyBrowser.Driver.Legacy</RootNamespace>
        <AssemblyName>MyBrowser.Driver.Legacy</AssemblyName>
        <!-- Output to solution-relative bin/Runtimes/Legacy -->
        <OutputPath>$(SolutionDir)bin\$(Configuration)\net8.0\Runtimes\Legacy\</OutputPath>
        <AppendTargetFrameworkToOutputPath>false</AppendTargetFrameworkToOutputPath>
    </PropertyGroup>

    <ItemGroup>
        <ProjectReference Include="..\MyBrowser.Abstractions\MyBrowser.Abstractions.csproj" />
    </ItemGroup>

    <!-- Verify output directory before build -->
    <Target Name="VerifyPhysicalIsolation" BeforeTargets="Build">
        <Message Text="Legacy Driver Output: $(OutputPath)" Importance="high"/>
        <MakeDir Directories="$(OutputPath)" />
    </Target>

    <!-- Copy driver to Runtimes folder after build -->
    <Target Name="CopyToRuntimes" AfterTargets="PostBuild">
        <Copy SourceFiles="$(TargetPath)" 
              DestinationFolder="$(OutputPath)" 
              SkipUnchangedFiles="true" />
        <Message Text="Legacy driver copied to: $(OutputPath)" Importance="high"/>
    </Target>
</Project>